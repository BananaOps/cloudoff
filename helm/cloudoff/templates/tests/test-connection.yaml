apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloudoff.fullname" . }}-test-connection"
  labels:
    {{- include "cloudoff.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloudoff.fullname" . }}:{{ .Values.service.port }}/status']
  restartPolicy: Never
